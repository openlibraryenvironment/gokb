node {

  stage('Test') {
    sh 'server/gokbg3/gradlew --no-daemon --console plain clean test-app'
  }

  stage('Build') {
    sh 'server/gokbg3/gradlew --no-daemon --console plain clean war'
    exports = findFiles glob: 'server/gokbg3/build/libs/*.jar'
  }

}
